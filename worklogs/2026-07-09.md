# 2026-07-09 工作紀錄

## 完成進度

- 建立 icon 資源流程：
  - 新增 `src/styles/my.icon.css` 作為 icon class 定義來源。
  - 將 `my.icon.css` 用到的 icon 從 `src/resource` 複製到 `src/images`。
  - 設定暫存用的 `resource` 不進 dist，也不提交到 GitHub。
  - datebox icon 統一改用 `icon-datebox`，後續 icon 定義以 `my.icon.css` class 為準。
- 完成 editor 自訂 icon button：
  - edit cell 支援 `icons: [{ iconCls, onClick }]`。
  - 只有原本沒有內建 icon 的 editor 可套自訂 icon；datebox、combobox 等內建 icon 不被自訂 icon 覆蓋。
  - demo 加入 `icon-refwin` 範例。
- 完成 popup lookup demo：
  - demo 新增 `Popup 參照` 欄位。
  - 點擊自訂 icon 會開啟 lookup popup。
  - popup 內容使用 FabGrid 建立。
  - 按確定或在 popup grid 資料列雙擊，會取選取 row 第一欄值帶回 edit cell。
  - demo 的 `Popup 參照` 欄會隨機填入 lookup grid 第一欄資料，方便測試。
- 調整 editor icon 位置：
  - 修正 datebox / refwin icon 下方超出 edit cell 的問題。
  - icon 右側保留 2px。
  - 修正 icon 右側留白處出現多餘線條的問題。
- 調整開發流程：
  - 確認日常修改不主動編譯 `dist`。
  - 新增並使用 `demo/dev-grid.html` 直接測試 `src` 下的 source code。
  - 保留 `demo/grid.html` 作為 dist-mode demo。
- 完成搜尋列：
  - FabGrid 新增 `showSearchRow` 開關。
  - 搜尋列高度與資料列一致，預設 32px。
  - 搜尋列 input focus 背景與 edit cell 一致。
  - 搜尋列 input border 改為 dotted。
  - 搜尋列左方做成獨立 cell，並可顯示 filter 後 rows count。
  - 搜尋列最左方 cell 雙擊可清除所有搜尋條件。
  - 搜尋列支援 `searchDelay`，預設 `200` ms。
  - Enter / Tab 移到下一個 search input，Shift+Enter / Shift+Tab 移到上一個，到頭或到底不動作。
  - search input 的 `text-align` 會跟 column 設定一致。
- 完成欄位搜尋比對與 filter operator：
  - 文字欄預設開頭比對。
  - 數字欄預設等於比對。
  - 日期欄預設開頭比對，datebox filter popup 與數字欄相同。
  - filter icon 顯示在 header cell 右上角。
  - 點擊 filter icon 可開啟 operator popup。
  - 選取 operator 後，header filter icon 會用 operator 符號替代，並以淡藍色顯示。
  - popup 最後一項可清除 operator 並恢復 filter icon。
  - 清除 icon 使用 `images/clear.png`。
  - 搜尋列關閉時會隱藏 filter icon。
- 完成 popup menu 視覺規則：
  - filter operator popup 改為統一 popup menu 樣式。
  - 左側 icon 欄與文字欄之間加入分隔線。
  - 之後 popup menu 應沿用同一視覺規則，已記錄到 `AGENTS.md`。
- 完成 search input icon 支援：
  - search input 支援與 edit cell 相同的 `icons: [{ iconCls, onClick }]` 定義。
  - demo 在 `Popup 參照` 欄位的 search input 加入 lookup icon。
  - 修正 search input icon 上下多餘框線。
- 完成 datebox / combobox 欄位的 search input 行為：
  - datebox search input 沿用欄位 editor 的 mask 與 icon，但不做 validate。
  - datebox search input 有遮罩行為。
  - combobox search input 沿用欄位 editor 的 icon 與選單，但不做 validate。
  - combobox search input 輸入 value 後按 Enter 會轉成顯示文字，例如 `1` 轉成 `買賣`。
- 修正 selection 與捲動：
  - 單選時 click cell 不再觸發 unselect row。
  - 單選時保持至少一列 selected，並顯示 selected row 背景。
  - 方向鍵向下移動時，selected row 保持在可視範圍內。
  - 修正底部出現半列或空白列的捲動對齊問題。
- 調整 row header 定義：
  - `showRowHeaders: false` 關閉。
  - `showRowHeaders: true` 顯示列號。
  - `showRowHeaders: 'cell'` 只顯示左側 cell，寬度為 18px。
- 補強狀態欄示範：
  - 恢復狀態欄文字顏色。
  - 狀態欄 search input 不套用狀態欄顏色。
- 修正 filter 到無資料時 editor popup 殘留：
  - filter 後若正在編輯的 row 已不在 view，會清除 editor state。
  - 同時關閉 editor、editor icon host、datebox panel、combobox panel。
  - 搜尋列焦點仍保留在 input 上。
- 補強多國語言檢查：
  - 已確認核心 locale key 在 `en`、`zh-TW`、`zh-CN` 三個檔案中一致。
  - 發現 combobox empty text 仍寫死繁中，demo lookup popup 與 demo 部分按鈕文字也尚未完全多國語言化。
  - 已將「新增 UI 文字要同步補 locale」記錄到 `AGENTS.md` 與 `README.md`。
- 更新文件：
  - `AGENTS.md` 補上 source-mode demo、locale、popup menu 長期規則。
  - `README.md` 補上搜尋列、filter operator、自訂 icon 與今日開發經驗。
  - 新增本工作紀錄。

## 驗證

- `node --check src/fabgrid-core.js` 通過。
- `node --check demo/js/grid.js` 通過。
- `node --check src/locales/fabgrid-locale.en.js` 通過。
- `node --check src/locales/fabgrid-locale.zh-TW.js` 通過。
- `node --check src/locales/fabgrid-locale.zh-CN.js` 通過。
- 用 `demo/dev-grid.html` 驗證 source-mode 開發流程。
- 用瀏覽器驗證 filter 到 0 筆時，editor 與 combobox/datebox popup 會關閉，且 search input 焦點保留。

## 備註

- 今日多數修改屬於 source / demo 行為調整；只有在使用者明確要求時才編譯 `dist`。
- 目前仍有待補項目：
  - combobox empty text 要改走 locale。
  - demo lookup popup title、button、pager/count 與 icon title/ariaLabel 要改走 demo locale。
  - `demo/grid.html` / `demo/dev-grid.html` 的 row header select option 文字要在語言切換時更新。
