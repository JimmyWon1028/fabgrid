# 2026-07-13

## 完成進度

- 新增 `fabgrid-jquery` 獨立 wrapper package，以 `$.fn.fabgrid` 對應 pure JavaScript FabGrid core，未修改 core rendering 與 virtualization。
- 完成多元素初始化、instance 儲存、重複 options 更新、option getter/setter、公開方法 dispatch、chainability、事件轉接、取消語意與 destroy/recreate lifecycle。
- 新增 jQuery wrapper TypeScript declaration、獨立 build script、API 文件與 2000-row source-mode Demo。
- 新增 7 項 wrapper 單元測試，涵蓋初始化、更新、方法、事件、清理、重建及錯誤保護。
- 依日常 source 修改規則未執行 build，未更新 `packages/fabgrid-jquery/dist` 或 FabUI `dist`。
- 將 `dev-jquery-grid.html` 對齊 `dev-grid.html` 的完整工具列、統計、篩選、群組、分頁、remote、編輯與匯出功能；共用 `grid.js`，並由 jQuery wrapper 建立主 Grid 與 popup Grid。
- 依使用者指示將 jQuery wrapper 納入完整 `npm run build` 流程，產生 browser global 與 ESM 的一般版及壓縮版發佈檔。
- 新增 `demo/jquery-grid.html` build-mode 完整 Demo，引用 `dist/fabui.min.js` 與 `packages/fabgrid-jquery/dist/fabgrid-jquery.min.js`；Pages artifact 同步納入 jQuery wrapper dist。
- 新增 `demo/js/` 目錄，將共用 Demo scripts 與 Vue SFC entry 統一移入，並同步更新所有 HTML、Vue import 與 build 路徑。
- 新增 `demo/style/` 目錄，將共用 Demo CSS 統一移入，更新 HTML 與 Vue style import，並調整 icon 圖片的相對路徑。
- Wrapper browser 發佈入口統一新增至 `dist/wrapper/`，輸出 `fabgrid-jquery.min.js` 與 `fabgrid-vue.min.js`；Demo 與 Pages 改由 `dist` artifact 直接引用，package dist 仍保留完整 bundles。
- 修正搜尋列連續按 Enter 向右移動時 header 與 rows 錯位：header 裁切容器不得因 input focus 自行累積水平 scroll，搜尋欄聚焦改用 `preventScroll` 並維持 header/footer 內層 scrollLeft 為 0。
- 整理 `demo/js/grid.js`：拆分啟動流程、Grid options、模擬遠端 loader、Grid／工具列事件及共用 viewport 狀態讀取，補上英文區段與意圖註釋，維持既有 Demo 行為與載入方式。
- 新增 FabGrid `color` editor：支援色碼正規化與驗證、cell 色塊、預設／自訂色票、HSV 顏色區、色相與透明度控制；共用 Demo 新增顏色欄位，並同步補齊三語 locale、README、API 文件與測試。
- 將 `color` editor popup panel 底色調整為白色，並更新 source-mode Demo CSS 快取版本。
- 將 Demo 文字日期欄位的實際 binding 由 `textDate` 改為 `dlvdt`，同步更新共用資料、Pure JS／jQuery／Vue Demo 與快取版本。
- 將 Demo 主要廠商欄位的實際 binding 由 `id` 改為 `facno`，同步更新列群組設定及 Pure JS／jQuery／Vue Demo。
- 移除 Demo 幣別欄位的額外文字樣式，並將草稿、待審核、已核准、已結案四種狀態改用不同文字顏色顯示。
- 將 FabGrid 所有資料 cell 的左右 padding 調整為 `7px`、一般 cell editor 調整為 `6px`，並更新 source-mode Demo CSS 快取版本；帶圖示的 editor 保留必要的圖示預留空間。
- 修正 color editor panel 在 rows 捲動後停留於舊位置：捲動時比照 datebox／combobox 關閉 panel，並改用正確的 `flex` 開啟狀態處理 trigger 收合。
- 修正 `handleEditorBeforeInput()` 在 config 初始化前讀取 `type` 的錯誤，並將 color editor 外觀與 panel 同步移至值更新後的 `input` handler。
- color editor 新增標準 CSS 顏色名稱支援，可輸入名稱或 hex 並即時預覽；同步更新 API、README、專案契約與測試。
- 同步更新 `src/fabui.js` 對 editor definitions 與 FabGrid 子模組的 query version，避免 source-mode Demo 只更新公開入口、內部模組仍命中舊快取。
- 修正 header search 延遲 callback 漏綁 Grid instance：`scheduleHeaderSearch()` 補上 `self = this`，避免 combobox 搜尋選值後誤把瀏覽器 `window.self` 當成 Grid。
- 依最新需求調整 color editor：顏色名稱仍可驗證與預覽，但提交後保留原名稱；hex 維持既有正規化，cell rendering 與再次編輯不再把名稱轉成 hex。
- 欄位搜尋列新增 color 下拉 trigger，沿用 60 色色票與 HSV panel；選色會寫回搜尋 input 並觸發 debounce filter，搜尋情境不執行 cell validation。瀏覽器驗證 `#ff0000` 正確篩選 2,500 筆、無效文字只產生 0 筆結果且不顯示 validation，38 項測試通過。
