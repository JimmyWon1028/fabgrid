# 2026-07-20

## 完成進度

- 新增 `FabGrid.setHeaderCellStyle(binding, style)`：以欄位 binding 設定 Header cell style object，保留原有 Grid 與 `formatItem` 樣式，同名 CSS property 由指定值覆蓋；支援 camelCase、kebab-case、CSS custom property、傳入 `null` 清除，以及無效 binding／style 回傳 `false`。同步更新 API、README、AGENTS 與回歸測試，398 項單元測試及 source-mode Grid 瀏覽器載入驗證通過，本批未重建 `dist`。
- 修正 `dev-grid.html` 與正式 Grid Demo 的左右凍結欄數字控制：Number EditBox 輸入時直接把可視 editor 的 `input` 事件橋接回原始欄位，避免 value bridge 的 silent 同步提前吃掉 `onChange`，讓 `setFrozenColumns()`／`setFrozenRightColumns()` 隨數字改變立即生效；新增 Demo bridge 回歸測試，399 項單元測試及左右凍結欄瀏覽器操作驗證通過，本批未重建 `dist`。
- 新增 source-mode `demo/dev-win7.html`，以簡易 Windows 7 桌面外殼組合 `fabui.Window`、`fabui.Layout`、`fabui.Tree`、`fabui.Tabs`、`fabui.FabGrid` 與 `fabui.Button`；包含左側桌面捷徑、中央 Computer 視窗、資料表、About 頁籤與底部工作列，頁面樣式限制在 `win7-*` 命名空間以避免污染元件。同步加入開發版 Demo 索引與組合契約測試，400 項單元測試及瀏覽器操作／畫面驗證通過，本批未重建 `dist`。
- 強化 Windows 7 Desktop Demo：工作列時間改為每秒更新，桌面捷徑支援 Pointer Events 拖曳並限制在工作列上方，三個桌面圖示改用 `.icon-win7-*` class 定義；Computer 視窗最小化後隱藏到工作列，點擊工作列 Computer 的 `fabui.Button` 可還原視窗。400 項單元測試及時間、拖曳、最小化／還原瀏覽器操作驗證通過，本批未重建 `dist`。
- 調整 Windows 7 Desktop 工作列：移除 Start 右側常駐的 Computer／Network／About 模擬按鈕；點 Start 以 `fabui.Menu` 顯示 Computer、Documents、Network、所有程式、說明及關機功能表。Computer 視窗最小化時才在 Start 右邊顯示工作列 `fabui.Button`，點擊還原後立即隱藏；另以限定 `.win7-task-button[hidden]` 的規則處理 FabUI Button display 與原生 hidden 的樣式衝突。400 項單元測試及 Start Menu、最小化工作列按鈕、還原移除按鈕的瀏覽器操作驗證通過，本批未重建 `dist`。
- 將 Windows 7 Desktop 的三個桌面捷徑拆成獨立視窗與不同內容：Computer 維持 Layout／Tree／Tabs／FabGrid，Network 使用獨立 FabGrid 顯示裝置、IP 與狀態，Monitor 使用 `fabui.Chart` 顯示 CPU、Memory 與 Disk 使用率；三個 `fabui.Window` 各自支援開啟、關閉、最小化與對應的動態工作列 Button。400 項單元測試及三個視窗開啟、不同內容、Monitor 最小化／工作列還原的瀏覽器驗證通過，本批未重建 `dist`。
- 新增 Windows 7 Desktop 空白區右鍵主題選單：以 `fabui.Menu` 提供完整 16 組 FabUI 主題，選取後同步更新三個 Window、Layout、Tree、Tabs、Chart、Start Menu 與工作列 Button；視窗、工作列、既有 Menu 與桌面捷徑不觸發桌面主題選單，另支援 Shift+F10／Context Menu 鍵。400 項單元測試及主題選單開啟、Black 主題同步至三個 Window 與 Layout 的瀏覽器驗證通過，本批未重建 `dist`。
- 新增正式版 `demo/win7.html`，使用 `dist/fabui.min.js` 與 `dist/fabui.css`，並與 source-mode `dev-win7.html` 共用完整 Windows 7 Desktop 展示；同步加入正式版 `index.html` 與開發版索引的正式版欄位。已完成 FabUI core／theme build，400 項單元測試及正式版三個視窗瀏覽器驗證通過。
- 修正正式版 bundle 的 Window 右上角工具 icon：將 `.fui-window-tool` 的 `background` shorthand 改為 `background-color`，避免 bundle 中後載入的 Window 基礎規則清除 `fabui.icon.css` 提供的 `panel_tools.png` sprite；新增回歸測試並重建 `dist`。400 項單元測試及正式版最小化／最大化／關閉 icon computed style 驗證通過。
- 修正 Windows 7 Desktop 視窗最小化動畫：Demo 的 `onMinimize` 不再立即隱藏 Window，而是先讓 `fabui.Window` 完成既有的 180ms 幾何過渡，再收進工作列；動畫途中還原或關閉會取消延遲隱藏，並保留 `prefers-reduced-motion` 行為。同步更新 source／build Demo query version 與回歸測試，已完成 FabUI core／theme build，400 項單元測試及正式版最小化／工作列還原流程驗證通過。
- Windows 7 Desktop 右鍵選單切換主題後以 `localStorage` 記憶，重新載入時驗證儲存值仍屬於支援的 16 組 theme 後，自動同步套用至 body、三個 Window 與其餘 FabUI 元件；儲存不可用或值無效時安全退回 `default`。同步更新 source／build Demo query version 與回歸測試，401 項單元測試、正式版 Black 主題跨 reload 及恢復 Default 的瀏覽器驗證通過，本批未重建 `dist`。
- `fabui.Window` 新增 `minimizeTarget`，支援 Element、selector、rect 或 function，最小化與還原動畫可精確對準單一目標矩形；Win7 Demo 的 Computer／Network／Monitor 各自綁定對應的工作列 Button，不再縮至 220×38px 左下角範圍。同步更新 README、AGENTS、Window API、source／build Demo query version 與回歸測試並完成 FabUI core／theme build；402 項單元測試通過，source-mode 與 build-mode 瀏覽器均驗證 Computer Window 終點 `97×30` 與 Button 實際 `97.47×30` 僅有整數像素取整差異，工作列還原後回復 `780×480`。
