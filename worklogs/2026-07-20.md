## 完成進度

- 新增 `FabGrid.setHeaderCellStyle(binding, style)`：以欄位 binding 設定 Header cell style object，保留原有 Grid 與 `formatItem` 樣式，同名 CSS property 由指定值覆蓋；支援 camelCase、kebab-case、CSS custom property、傳入 `null` 清除，以及無效 binding／style 回傳 `false`。
- 修正 `dev-grid.html` 與正式 Grid Demo 的左右凍結欄數字控制：Number EditBox 輸入時直接把可視 editor 的 `input` 事件橋接回原始欄位，避免 value bridge 的 silent 同步提前吃掉 `onChange`，讓 `setFrozenColumns()`／`setFrozenRightColumns()` 隨數字改變立即生效。
- 新增 source-mode `demo/dev-win7.html`，以簡易 Windows 7 桌面外殼組合 `fabui.Window`、`fabui.Layout`、`fabui.Tree`、`fabui.Tabs`、`fabui.FabGrid` 與 `fabui.Button`；包含左側桌面捷徑、中央 Computer 視窗、資料表、About 頁籤與底部工作列，頁面樣式限制在 `win7-*` 命名空間以避免污染元件。
- 強化 Windows 7 Desktop Demo：工作列時間改為每秒更新，桌面捷徑支援 Pointer Events 拖曳並限制在工作列上方，三個桌面圖示改用 `.icon-win7-*` class 定義；Computer 視窗最小化後隱藏到工作列，點擊工作列 Computer 的 `fabui.Button` 可還原視窗。
- 調整 Windows 7 Desktop 工作列：移除 Start 右側常駐的 Computer／Network／About 模擬按鈕；點 Start 以 `fabui.Menu` 顯示 Computer、Documents、Network、所有程式、說明及關機功能表。Computer 視窗最小化時才在 Start 右邊顯示工作列 `fabui.Button`，點擊還原後立即隱藏；另以限定 `.win7-task-button[hidden]` 的規則處理 FabUI Button display 與原生 hidden 的樣式衝突。
- 將 Windows 7 Desktop 的三個桌面捷徑拆成獨立視窗與不同內容：Computer 維持 Layout／Tree／Tabs／FabGrid，Network 使用獨立 FabGrid 顯示裝置、IP 與狀態，Monitor 使用 `fabui.Chart` 顯示 CPU、Memory 與 Disk 使用率；三個 `fabui.Window` 各自支援開啟、關閉、最小化與對應的動態工作列 Button。
- 新增 Windows 7 Desktop 空白區右鍵主題選單：以 `fabui.Menu` 提供完整 16 組 FabUI 主題，選取後同步更新三個 Window、Layout、Tree、Tabs、Chart、Start Menu 與工作列 Button；視窗、工作列、既有 Menu 與桌面捷徑不觸發桌面主題選單，另支援 Shift+F10／Context Menu 鍵。
- 新增正式版 `demo/win7.html`，使用 `dist/fabui.min.js` 與 `dist/fabui.css`，並與 source-mode `dev-win7.html` 共用完整 Windows 7 Desktop 展示；同步加入正式版 `index.html` 與開發版索引的正式版欄位。
- 修正正式版 bundle 的 Window 右上角工具 icon：將 `.fui-window-tool` 的 `background` shorthand 改為 `background-color`，避免 bundle 中後載入的 Window 基礎規則清除 `fabui.icon.css` 提供的 `panel_tools.png` sprite。
- 修正 Windows 7 Desktop 視窗最小化動畫：Demo 的 `onMinimize` 不再立即隱藏 Window，而是先讓 `fabui.Window` 完成既有的 180ms 幾何過渡，再收進工作列；動畫途中還原或關閉會取消延遲隱藏，並保留 `prefers-reduced-motion` 行為。
- Windows 7 Desktop 右鍵選單切換主題後以 `localStorage` 記憶，重新載入時驗證儲存值仍屬於支援的 16 組 theme 後，自動同步套用至 body、三個 Window 與其餘 FabUI 元件；儲存不可用或值無效時安全退回 `default`。
- `fabui.Window` 新增 `minimizeTarget`，支援 Element、selector、rect 或 function，最小化與還原動畫可精確對準單一目標矩形；Win7 Demo 的 Computer／Network／Monitor 各自綁定對應的工作列 Button，不再縮至 220×38px 左下角範圍。
- 修正 `fabui.Diagram.setPaper()` 切換直向／橫向時只更新 SVG 顯示尺寸、未同步 `viewBox` 的問題；現在每次重繪都會依目前紙張寬高更新畫布座標系，避免直向紙張被橫向比例壓窄、圖形顯示到灰色工作區。
- `fabui.Diagram` 新增 `fitToPage()`，依 viewport、紙張尺寸與 zoom 範圍計算整頁縮放，不使用固定比例；Diagram Demo 初次開啟、還原範例與切換紙張方向後都會自動顯示完整紙張。
- `fabui.Diagram` connector 支援直接拖曳並顯示中段控制點；以 `controlX`／`controlY` 保存路徑控制資料，直線拖曳形成折點、直角線移動中段、弧線拖曳改變二次貝茲曲線的彎曲方向與弧度，並沿用既有 pointer lifecycle、JSON、undo／redo。Demo 新增 curved connector 範例。
- Diagram 紙張在小於 viewport 時改為水平與垂直置中，SVG 使用不可縮小的 flex item 與 auto margin；放大超過 viewport 時 margin 自動歸零並保留完整捲動範圍。放大至 113% 後 scroll size 與紙張尺寸一致。
- 新增 `fabui.lite.*` 精簡發佈組合，只包含 FabGrid、內建 TreeGrid、Pivot、Chart 與必要的 Control、editor definitions、三語系、popup、theme／icon 依賴；提供 browser global、ES module、CSS 與各自壓縮版本，並新增 `npm run build:lite`，可在不清除完整 `dist/fabui.*` 的情況下獨立重建 Lite 輸出。
- `demo/dev.html` 的 Demo／開發版／正式版三欄索引只允許由第一欄 Demo 項目啟動原生上下拖曳排序，開發版與正式版連結明確禁止拖曳，滑鼠移入連結時維持一般箭頭指標；拖曳期間顯示插入位置線，放開後立即調整當次頁面順序，並依使用者於瀏覽器最新排定的 33 項順序更新原始 HTML 預設排列。`demo/index.html` 同步依此順序排列 31 個具有正式版連結的項目；兩個索引都維持一般文字連結且不載入 FabUI CSS／JavaScript。
- `demo/dev-diagram.html` 初次載入與還原範例時，會依目前 A4 橫向紙張尺寸將整組流程圖節點邊界置中，再沿用 `fitToPage()` 顯示完整紙張；此開發版專用 option 不改變正式版 `demo/diagram.html` 的既有初始資料位置。
- `fabui.Diagram` 工具列的復原／重做按鈕由 `↶`／`↷` 文字符號改為 FabUI icon-only Button，沿用 `src/theme/images/undo.png`／`redo.png` 並在 `src/fabui.icon.css` 集中定義；三語系 title 與 `aria-label`、disabled 狀態及既有 Undo／Redo 行為維持不變。
- `fabui.Diagram` 做圖區新增右鍵 `fabui.Menu`，提供三語系「匯出 PNG／匯出 SVG／列印」；新增 `exportPng(filename?, scale?)` Promise API，以預設 2 倍解析度輸出紙張 PNG，並新增 `print()` 列印目前紙張 SVG。選單沿用 Menu 的 theme、Escape、外部點擊與 dispose lifecycle，工具箱及屬性區不觸發。
- `fabui.Diagram` 工具箱新增「組織圖」分類，以及圖片在左側、右側、上方的三種 SVG 人員卡片；預覽沿用 Diagram shape renderer，拖入畫布後支援一般 node 的拖曳、八方向縮放、六點連線、屬性與圖形內文字編輯。
- `fabui.Diagram` 做圖區右鍵選單新增全螢幕／離開全螢幕，fullscreen target 改為畫布 viewport，工具箱、屬性區、主工具列與下方檢視工具列不進入全螢幕；Menu 會在 fullscreen top layer 內維持可見，離開後還原至 `body`，並加入 standard／WebKit／`:fullscreen` 狀態相容判斷。
- `fabui.Diagram` 工具箱分類的疊合／展開箭頭由 12px 放大為 20px；工具箱右側新增 6px splitter，可在 140–420px 間拖曳調整，並支援方向鍵 10px、Shift+方向鍵 30px 調整與 ARIA value。Document pointer listener 只在拖曳期間存在，pointer cancel 會還原原寬度。
- 統一 `dist/` 樣式發佈命名：16 組獨立主題由 `dist/theme/fabgrid.<theme>.css`／`.min.css` 改為 `dist/theme/fabui.<theme>.css`／`.min.css`；後續 build 不再產生舊 `fabgrid.*.css` 檔名。
- 同步修正所有引用正式樣式的 Demo 與 Vue 2 範例，統一載入新版 `dist/fabui.css`，不再引用 `src/theme/fabgrid*.css` 或 `dist/theme/fabgrid*.css` 舊命名。
- `fabui.Diagram.print()` 改用目前頁面的隱藏 iframe 載入紙張 SVG 並叫出瀏覽器列印介面，做圖區右鍵「列印」不再開啟新分頁或新視窗；列印完成後由 `afterprint` 自動移除暫存 iframe，另保留逾時清理。
- `fabui.Diagram` 全螢幕改為投影片展示：在 fullscreen viewport 尺寸生效後，以 24px 舞台留白重新縮放並置中完整紙張；全螢幕期間以限定 lifecycle 的 `ResizeObserver` 跟隨瀏覽器最終 viewport 尺寸，進入前保存 zoom 與 scroll，離開後解除 observer 並自動還原，另補上 WebKit fullscreen change lifecycle。
- `fabui.Diagram.print()` 的隱藏列印文件新增 `@page size`，同步帶入目前 `paperSize` 與 `paperOrientation`；A3／A4／A5／Letter／Legal 的橫向與直向列印預覽會跟做圖區紙張一致。
- 修正 Diagram 列印預覽多出空白第二頁：列印 CSS 以 `!important` 覆蓋 SVG 匯出內容的固定 inline 像素寬高，將 SVG 同時限制在單頁可列印寬度與高度內，並以 flex 等比例置中、隱藏溢出及禁止圖形內分頁。
- Diagram 列印預覽預設不顯示瀏覽器 header／footer：`@page` 改為零頁邊距，原本 12mm 留白移至 border-box body padding，保留圖面安全邊界並維持單頁方向與縮放契約。
- Diagram 列印預覽預設移除網格：列印專用 SVG 副本將 page fill 還原為目前 `pageColor` 並移除未使用的 grid pattern；做圖區 `showGrid` 狀態與 SVG／PNG 匯出結果維持不變。
- `fabui.Diagram` 工具箱預設寬度依 Demo 實際調整值改為 260px；分類初始狀態改為只展開第一個「一般」，其餘「流程圖／DFD／組織圖」疊合。新增 `toolboxCollapsed` 與 opt-in `toolboxStateKey`，Demo 會將每個分類最後的展開／疊合狀態保存並於重新載入時恢復。
- `fabui.Diagram` 上方工具列新增三語系「清除頁面」Button，沿用 `icon-clear`；新增 `clearPage()` API，清除所有圖形與連線但保留紙張設定，空白頁面時自動停用，並納入既有 history 以支援復原／重做。
- Diagram 復原圖示改用 `res/images/undo.png` 的藍色 16×16 PNG，並將該圖精確左右鏡射產生 `res/images/redo.png`；兩張圖已複製替換 `src/theme/images/undo.png`／`redo.png`，正式 source 不直接依賴 `res/`，並更新 CSS asset cache key。
- 修正 Diagram 在 73% 等非 100% zoom 下雙擊圖形時文字 EditBox 向左上偏移：定位現在除 SVG 內座標與 zoom 外，也納入 viewport scroll、邊框及置中紙張的實際 SVG offset；node 與 connector 共用相同換算。
- 修正組織圖卡片拖曳後自動 connector 可能投影到圖形角落或沿邊框繞行：未指定固定連接點時，改以四個自動邊中點評分距離與朝向，每次重繪重新選點；直角線依起訖端點邊向正確方向出入，明確指定的六個 `fromPoint`／`toPoint` 仍保持不變。
- Diagram connector 新增 `arrowDirection`，選取線條後可在屬性區切換無箭頭、指向終點、指向起點或雙向箭頭；SVG 依選項套用 `marker-start`／`marker-end`，預設維持指向終點，並沿用既有 JSON 與 undo／redo lifecycle。
- Diagram 工具列「連線」改為十字指標拖曳模式：啟用時不再移動或縮放圖形，可從 A 圖形內任意位置拖至 B 圖形內任意位置；拖曳預覽會吸附兩圖形的最佳邊中點，放開後建立自動端點 connector，同一圖形、空白處與 pointer cancel 不新增連線。
- Diagram 上方工具列新增三語系「工具箱」toggle Button，可開啟／關閉左側工具箱與 splitter；按鈕 pressed 狀態跟隨 `options.toolbox`，重新開啟時保留工具箱寬度及分類展開／疊合狀態。
- Diagram 工具箱的「一般／流程圖／DFD／組織圖」分類標題右側新增 `fabui.Button` icon-only 拖曳把手；可上下拖曳重排分類，顯示 55% 半透明插入線，並由既有 `toolboxStateKey` 同時保存分類順序與展開／疊合狀態，舊版儲存格式仍可載入。
- Diagram 全螢幕拆分為兩種明確行為：右下角檢視工具列的「全螢幕」改以完整 Diagram host 顯示主工具列、工具箱、屬性區、做圖區與檢視工具列；做圖區右鍵「全螢幕」改呼叫新增的 `togglePresentationFullscreen()`，維持只有紙張畫布的投影片展示。兩者都會依可用 viewport 執行 `fitToPage(24)`，退出時還原原本 zoom／scroll。
- Diagram 上方工具列新增三語系「下載／載入」FabUI Button：下載以既有 `export()` 產生 `fabui-diagram.json`，載入透過只接受 JSON 的隱藏 file input 與 FileReader，成功後沿用 `import()`／`setData()`、觸發 `JsonLoaded` 並 `fitToPage()`；無效內容觸發 `LoadError` 且不改變目前圖面，dispose 時會中止仍在讀取的檔案。
- Diagram 工具箱分類標題右側的排序把手由六點式圖示簡化為三條 12px 短橫線，使用單一 `repeating-linear-gradient` 並保留原本 32×34px Button 操作範圍、上下拖曳排序與狀態記憶行為。
- Diagram node 新增 `hyperlink` 屬性；選取圖形後，屬性區在「文字」正下方顯示三語系「超連結」EditBox。有效網址會讓圖形文字顯示底線與手形指標，單擊以 `noopener,noreferrer` 新分頁開啟並觸發 `HyperlinkClick`；支援 HTTP／HTTPS／mailto／tel、相對網址與頁內錨點，未指定協定時補上 HTTPS，其他協定不啟用。超連結保存於 JSON 並支援 undo／redo；有連結時仍可雙擊文字以外的圖形區域編輯文字。
- Diagram 做圖區右鍵選單的「全螢幕／離開全螢幕」改名為「投影片展示／離開投影片展示」，英文與簡體中文同步改為 Slide show／幻灯片放映；右下角檢視工具列仍維持「全螢幕」並顯示完整編輯器，右鍵行為仍只顯示紙張 viewport。
- 修正 Diagram 完整編輯器全螢幕在自動符合頁面約 94% 後無法繼續放大：fullscreen `ResizeObserver` 現在比較 viewport border box，手動 zoom 導致捲軸出現或 content box 改變時不再誤觸 `fitToPage(24)`；只有瀏覽器或 fullscreen viewport 外框真的改變尺寸才重新符合頁面。
- 修正 FabGrid 空資料時調整視窗大小可能在 `renderPagination()` 寫入未定義 `style` 的錯誤：pager 設定與內部分頁 DOM 改用獨立參照，外部覆寫相容用 `pager` alias 後，resize 排程 render 仍使用穩定 DOM。
- 修正 FabGrid 初始化時資料為空且 host 尚未取得可用寬度時，只顯示凍結欄 header、必須手動 `refresh()` 才顯示非凍結欄的問題：FabGrid 現在以 `ResizeObserver` 監聽 host 尺寸，首次 layout 完成及容器尺寸改變時自動排程 render，dispose 時立即解除 observer。
- 修正 Diagram 左下角誤顯示原生「Choose File」欄位：Demo 共用 FabUI 控件增強器現在略過具有 `hidden` 的 file input，Diagram 內部 JSON 載入欄位維持隱藏，工具列「載入」按鈕仍正常保留。
- 修正 Diagram 右側屬性 EditBox 輸入 Backspace／Delete 時誤刪選取圖形或連線：Diagram 全域鍵盤捷徑現在會略過 input、textarea、select、button、link、contenteditable 與對應 ARIA 互動控件，文字、超連結、X／Y、寬高、填色、框線、線型、箭頭方向、工具箱搜尋及就地文字輸入皆保留原生鍵盤行為。
- Diagram node 超連結新增 `javascript:` script URL 支援；`javascript:alert('12345')` 等內容會在目前 Diagram 頁面執行，一般 HTTP／HTTPS、mailto、tel、相對網址與頁內錨點仍使用新分頁開啟，兩種行為都觸發 `HyperlinkClick`，其他未支援協定維持阻擋。同步標明 script URL 可執行任意頁面程式、只能使用可信任內容。
- Diagram node 新增 `hyperlinkTrigger: 'click' | 'dblclick'`，預設 `click`；右側屬性在「超連結」下一列提供三語系「單擊觸發／雙擊觸發」Combo，設定會保存於 JSON 並沿用 property undo／redo。符合設定的文字單擊或雙擊才開啟／執行連結，觸發超連結時不建立就地文字 EditBox，圖形文字外仍可雙擊編輯。
- 修正未選取 Diagram node 時第一次點超連結文字只完成選取、必須再點一次才觸發的問題：單擊模式改在未拖曳的 pointer release 立即開啟／執行一次，再攔截後續 click 避免重複；雙擊模式也可從未選取狀態直接雙擊文字觸發。
- Diagram 上方工具列新增三語系「唯讀」toggle Button，直接呼叫 `setReadOnly()` 控制作圖區並同步 pressed 狀態；狀態改變時觸發 `ReadOnlyChanged`，Demo 上方既有唯讀 CheckBox 也會雙向同步。唯讀時主工具列保持可見以便解除鎖定，會改動畫面的按鈕與復原／重做快捷鍵停用，工具箱及屬性區隱藏，下載、縮放、格線、全螢幕與右鍵檢視功能保留。
- 修正 Diagram 點選圖形或連線後按鍵盤 `Delete`／`Backspace` 沒有反應：SVG pointer down 會將焦點留在 Diagram host，因此可直接使用刪除、方向鍵與復原／重做快捷鍵，不必再點外框；屬性 EditBox、搜尋及就地文字輸入仍取得自己的焦點並保留原生按鍵。
- Diagram 繪圖 viewport 新增滑鼠滾輪縮放：向上放大、向下縮小，以指標所在紙張位置為縮放錨點，連續觸控板滾動依 delta 平滑調整並限制於 `minZoom`／`maxZoom`；listener 明確使用非 passive 模式，只攔截繪圖區垂直滾輪，唯讀仍可縮放，工具箱與屬性區維持原本捲動。source-mode 開發頁已載入新版快取。
- FabGrid 初始化時若啟用 Search Row，現在會在本機初次 render 完成後自動聚焦第一個可見欄位的搜尋 input；遠端資料模式則等待初次 load 結束後再聚焦。Header 重繪前會保存目前搜尋 input 的欄位與游標範圍並在重繪後恢復，避免 ResizeObserver 首輪重繪或後續 refresh 移除焦點。
- Diagram 工具箱最上方標題新增整體面板拖曳：從左側停靠狀態往工作區拖曳會轉為浮動面板，浮動時可在工作區範圍內移動，拖回左側 56px 停靠提示區並放開會重新 dock；分類標題右側 icon 的上下排序維持獨立。浮動狀態保留原本 6px 寬度 splitter，pointer cancel 會還原開始前狀態與位置，document pointer listener 只在拖曳期間存在。新增 `toolboxFloating`／浮動座標 options 與 `isToolboxFloating()`／`floatToolbox()`／`dockToolbox()` API。
- Diagram 右側屬性面板標題新增整體拖曳：從右側停靠狀態往工作區拖曳會轉為浮動面板，浮動時可在工作區範圍內移動，拖回右側 56px 停靠提示區並放開會重新 dock；面板內 EditBox 與屬性操作不會啟動拖曳。Pointer cancel 會還原開始前狀態與位置，document pointer listener 只在拖曳期間存在。新增 `propertiesFloating`／浮動座標 options 與 `isPropertiesFloating()`／`floatProperties()`／`dockProperties()` API。
- Diagram 工具箱與屬性面板改為可停靠於左右任一側；拖到另一面板所在側時會自動交換位置，工具箱 splitter 也會跟隨工具箱移至對應內側。`dockToolbox(side?)`／`dockProperties(side?)` 新增 `'left'`／`'right'`，並提供 `toolboxDockSide`／`propertiesDockSide` options。既有 `toolboxStateKey` 儲存格式新增相容的 `panels` 區段，會記住兩個面板的顯示、浮動、停靠側、浮動位置與工具箱寬度，分類順序及展開狀態維持原格式；工具列開關、調寬、浮動移動及停靠完成後更新。
- FabGrid Search input 聚焦時，無 modifier 的 `ArrowUp`／`ArrowDown` 現在會移動 selected row 並自動捲動到該列；搜尋 input 保持焦點，即使 `editOnSelect` 啟用也不會進入 cell editor。已開啟的 Date／Combo／Color popup 仍優先處理方向鍵。
- 依最新操作契約修正 Search input 的向下鍵焦點：按 `ArrowDown` 移動 selected row 後，Search input 立即失焦並將焦點轉到 Grid；`ArrowUp` 仍保留 input 焦點，兩者在 Search input 觸發時都不啟動 cell editor。
- 修正 Search input 按上下方向鍵移動 selected row 時的 active cell 欄位：active cell 現在固定使用目前 Search input 的 column，不再沿用移動前其他欄位的 selection column；`ArrowDown` 的 Search input 失焦與 Grid 焦點轉移行為維持不變。
- 再調整 Search input 的 `ArrowDown` 進入 Grid 行為：第一次按向下鍵只將 active cell 切到目前 selected row 與 Search input 的同一 column，selected row 不移動，input 失焦並把焦點轉到 Grid；下一次方向鍵才開始一般 Grid 導航。
- 補齊 FabGrid 與 Search Row 的反向焦點切換：active cell 位於第一列時按無 modifier 的 `ArrowUp`，只有 Search Row 顯示且允許篩選時才將焦點交回同一 column 的 Search input；Search Row 隱藏或停用篩選時維持原本 Grid 導航。原 Search input `ArrowDown` 進入同列同欄 active cell 的流程也新增相同顯示狀態防護。
- 修正 Diagram 工具箱浮動時右側調寬 splitter 顯示成粗色帶的問題：保留 6px 滑鼠拖曳感應範圍，但可見邊界改為貼齊工具箱右框的 1px 細線；hover、鍵盤 focus 與拖曳期間只切換為主題 selection 色，不再填滿整個 splitter。
- 修正 Diagram 屬性面板 dock 到左側時缺少右框線：屬性面板現在依停靠側切換內側分隔線，左側顯示右框、右側顯示左框，浮動狀態維持完整外框。
- Diagram 工具箱與屬性面板統一改用透明雙邊調寬把手：dock 時只啟用朝畫布中央的 6px 邊緣，浮動時左右邊緣都可拖曳或用方向鍵調寬；工具箱 splitter 不再繪製任何分隔線。屬性面板新增 `propertiesWidth`／`propertiesMinWidth`／`propertiesMaxWidth` 與 `getPropertiesWidth()`／`setPropertiesWidth()`，兩個面板寬度都由 `toolboxStateKey` 保存，浮動面板由左邊調寬時會保持右邊位置。
- Diagram node 屬性面板將 `X`／`Y` 改為左右並排同一列，「寬度」／「高度」改為下一列左右並排；四個欄位仍各自使用既有 `fabui.EditBox` 與 property change／undo／redo lifecycle。
- Diagram 工具箱 dock 時補上朝畫布中央的 1px 面板框線：dock 左側顯示右框，dock 右側顯示左框；外側不加線，透明 6px 調寬 hit area 與浮動完整外框維持不變。
- 新增 `fabui.Accordion` pure JavaScript 核心；每個 pane 直接重用既有 `fabui.Panel`，提供 markup／programmatic panels、單一／多重展開、動態 add／remove、三方向 header、延遲載入、鍵盤、ARIA、三語系、16 組 theme、Control registry 與 dispose 還原。容器、一般／選取 header 色盤與 `accordion_arrows.png` 逐一對應本機 EasyUI theme 參考，source 不依賴 `res/`。
- Diagram 工具箱與屬性面板浮動時新增上下透明調高邊緣，配合既有左右調寬邊緣可由四邊調整尺寸；由左邊或上邊拖曳會固定另一側，支援方向鍵與 Shift 加速，並新增高度 options、getter／setter 及 `toolboxStateKey` 高度記憶。
- 修正 `fabui.Accordion` 箭頭 URL 跨 CSS 檔解析錯誤，header 現在依狀態明確顯示 EasyUI `accordion-expand`／`accordion-collapse` sprite；展開／收合新增 Panel 尺寸、Accordion flex 比例、內容透明度與 header 色彩的 180ms 過渡，並遵守 `prefers-reduced-motion`。
- 新增獨立 `fabui.Scheduler` pure JavaScript 附加套件；參考 Telerik Kendo UI Scheduler 的視覺與常用操作，但以 FabUI 自有 DOM、資料與事件流程實作 Day／Work Week／Week／Month／Year／Agenda／Timeline、本機 CRUD、全天行程、resource color、拖曳移動、結束時間調整、鍵盤、三語系與 16 組 theme。Scheduler 工具列與編輯器重用既有 Button、Window、EditBox、CheckBox，獨立 source／dist 入口固定為 `fabui.scheduler.*`，原 `fabui.*` core 不包含 Scheduler，且必須先載入。
- 新增獨立 `fabui.Gantt` pure JavaScript 附加套件；採 TreeList＋Timeline 資訊架構，提供階層任務、可調 Splitter、Day／Week／Month／Year、進度、摘要、里程碑、FS／FF／SS／SF 相依線、resource 標示、拖曳／縮放、鍵盤與 Task editor，並重用 FabUI Button、Window、EditBox。獨立 source／dist 入口固定為六個 `fabui.gantt.*`，原 `fabui.*` core 不包含 Gantt 且必須先載入。
- 修正 `dev-gantt.html` 與 `src/fabui.gantt.js` 使用不同 query URL 載入 `src/fabui.js`，造成瀏覽器建立兩份 ES module namespace、Gantt 掛載到非 Demo 使用的 `fabui` 實例；兩個入口現在共用完全相同的 core module URL。Gantt source factory 同時改由公開入口注入既有 Control registry 函式，不再要求瀏覽器快取中的 Control 必須具有後加的私有靜態別名。
- 依使用者附圖同步調整 Demo 索引順序：`FabGrid → FabGrid Remote → TreeGrid → fabui.Tree → fabui.Window → fabui.Layout → fabui.Panel → fabui.Accordion → fabui.Gantt`；`demo/index.html` 依相同正式版順序更新，其他項目順序維持不變。
- 依使用者最新指定只調整 `demo/dev.html`：將 `fabui.Gantt` 移到 `fabui.Diagram` 後，`fabui.Scheduler` 緊接在 Gantt 後，`FabUI Theme Builder` 仍維持最後；`demo/index.html` 保持不變。
- 依使用者後續指定讓 `demo/index.html` 跟隨 `demo/dev.html` 中具有正式版連結的項目順序：尾端統一為 `fabui.Diagram → fabui.Gantt → fabui.Scheduler → FabUI Theme Builder`。
- 修正 `fabui.Scheduler` Day／Work Week／Week 檢視中日期表頭、全天列與可捲動時間欄因垂直 scrollbar 寬度不同而逐欄偏移：時間區 render 後動態量測實際 scrollbar gutter，讓表頭與全天列保留相同右側寬度。
