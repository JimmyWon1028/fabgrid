# 2026-09-15

## 完成進度

- 改善 Cell 編輯期間使用上下方向鍵移動的效能；提交、選取、捲動與重新開啟 editor 維持原順序，過程中的重複 Grid render 合併為單次更新，降低長按方向鍵時的同步渲染負擔。
- 未修改 Cell 值而以方向鍵移動時，簡單本機 Grid 會沿用既有資料 view 與 layout；排序、篩選、群組、分頁、TreeGrid、遠端模式及 CollectionView 仍維持完整重建流程。
