## 完成進度

- 修正 FabGrid 在外層分頁隱藏時，零尺寸重繪與瀏覽器產生的原生 scroll reset 會清除定位；隱藏期間保留最後有效座標，重新顯示後再依實際位置與尺寸正常更新畫面。
- 新增 Column `allowResizing`，可針對單一欄位隱藏 resize 操作區，並阻止拖曳與 AutoFit 改變欄寬。
- 修正 CellRange 選取框使用實體 border 壓縮 cell 內容空間，造成點擊 cell 時文字位移；改用不影響盒模型的 inset shadow 繪製範圍外框。
