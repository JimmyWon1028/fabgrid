# 2026-07-06 工作紀錄

## 完成進度

- 完成 FastGrid V1 產品方向與架構規劃：
  - 明確定義 FastGrid 是 performance-first 的 pure JavaScript data grid。
  - 核心不依賴 Vue，Vue 後續只作為 wrapper。
  - V1 目標不是完整複製 Wijmo FlexGrid，而是先完成一般 grid 常用能力。
  - 確認雙向 virtualization 是核心要求，目標可朝 `20000 x 50` 大資料量擴充。
  - 確認 `frozenColumns` 是正式支援選項。
- 建立專案規範：
  - 撰寫 `AGENTS.md`，記錄產品方向、V1 範圍、效能要求、DOM 策略與打包需求。
  - 文件與專案對外說明統一使用繁體中文。
  - 代碼註釋維持英文，縮排使用兩個空白。
- 完成 FastGrid V1 core：
  - 在 `src/fastgrid-core.js` 建立 pure JS core。
  - 支援 `itemsSource`、`columns`、固定 row height、header、body、frozen pane。
  - 實作 row / column 雙向 virtualization，只渲染 viewport 與 buffer 範圍。
  - 實作 sorting、search/filter、column resizing、single cell selection、inline editing、CSV export。
  - 提供常用 API，例如 `setItemsSource()`、`setColumns()`、`setFrozenColumns()`、`refresh()`、`dispose()`。
- 完成 demo 與測試入口：
  - 建立純前端 demo，使用本地 mock data。
  - demo 以打包後的 `dist/fastgrid.min.js` 與 `dist/fastgrid.css` 驗證外部引用方式。
  - 建立 `test/smoke.html` 作為瀏覽器 smoke test 頁面。
- 完成 build pipeline：
  - 新增 `build/build.cjs`。
  - 從同一份 source 輸出 ES module 與 ES5 browser global 版本。
  - 產出 `dist/fastgrid.esm.js`、`dist/fastgrid.esm.min.js`、`dist/fastgrid.js`、`dist/fastgrid.min.js`、`dist/fastgrid.css`、`dist/fastgrid.min.css`。
- 完成 smoke runner：
  - 新增 `build/smoke.cjs`，由 script 自行啟動暫時 HTTP server。
  - 使用系統 Google Chrome headless 驗證 demo 行為。
  - 避免依賴需要額外瀏覽器下載的 Playwright binary。
- 完成專案改名：
  - 將專案資料夾從 `/Users/jimmywon/ai/datagrid` 改為 `/Users/jimmywon/ai/fastgrid`。
  - 更新 Codex 專案設定與 sidebar 相關 state，使專案名稱指向 `fastgrid`。
  - 將目前 Codex thread title 改為 `fastgrid`。

## 驗證

- `npm run build` 通過。
- `npm run smoke` 通過。
- smoke 結果確認：
  - browser global `FastGrid` 可用。
  - search 後資料列可正確過濾。
  - `setCellData` 可正確寫回資料。
  - rendered cells 受 viewport 限制，不會依完整 dataset size 全量渲染。
  - frozen pane 有正確渲染 frozen cells。

## 備註

- 此紀錄依據 Codex 既有 rollout memory 補寫，對應工作主題為 `fastgrid_pure_js_grid_build_rename`。
- 2026-07-06 的 rollout 原始 cwd 是 `/Users/jimmywon/ai/datagrid`；後續已改名為目前的 `/Users/jimmywon/ai/fastgrid`。
- 當時曾遇到 Playwright browser binary 不存在，因此 smoke runner 改用系統 Google Chrome headless。
- 專案改名後，Codex sidebar 可能有 app cache 延遲；若 UI 還顯示舊名稱，需 reload / restart Codex。
