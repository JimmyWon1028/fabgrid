## 完成進度

- FabGrid Header 只在文字與 Filter 空間本身不足時縮小外側間距；排序前後保持原有左側位置，Filter icon 固定，Sort icon 可移至其下方，未啟用 Filter 時文字可使用完整圖示空間。
- FabGrid Row hover 改為直接更新前後列的既有 DOM class，不再重建全部可見 Row 與 Cell，縮短滑鼠跨列反應時間。
- LYSM 生產進度的工序篩選／排序改為批次更新欄位可見性，並以資料索引取代重複全表掃描與逐筆響應式陣列更新。
