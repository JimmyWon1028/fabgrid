## 完成進度

- FabGrid 與 Chart 共用 `fabui.collections.CollectionView` 時，Grid 本機排序會同步更新 CollectionView 順序並觸發 Chart 自動重繪；使用端不需要修改既有程式碼。
- 更新 README 與 AGENTS，記錄一般本機 FabGrid 的排序、篩選、選取同步契約，以及 Remote Grid／TreeGrid 的排序範圍。
- 瀏覽器實測 Column、Line、Pie 的升冪與降冪順序皆與 Grid 一致；完整 633 項測試與 100 萬資料格 benchmark 通過。
- 重新執行 `npm run build:lite -- min`，只保留 `dist/fabui.lite.min.js` 與 `dist/fabui.lite.min.css`，未產生非壓縮或 ESM Lite 輸出。
- 執行 `npm run build:all`，完成 FabUI core、Lite、Diagram、Gantt、Scheduler 與 HtmlEditor 全部發佈輸出；633 項完整測試通過，所有 JavaScript 語法正確且未產生 ESM。
