# 2026-07-07 工作紀錄

## 完成進度

- 完成文字日期欄位的 maskedit 行為：
  - 欄位可設定 `mask`，例如 `9999/99/99`。
  - 使用者輸入時只需要輸入數字部分，遮罩分隔符會自動出現。
  - 支援設定遮罩字元是否屬於實際資料。
  - 當遮罩不屬於資料時，儲存值會保留原始數字，例如 `20260707`。
- 補強 masked field 的複製行為：
  - 一般 grid cell 複製會依欄位設定輸出 raw value。
  - 編輯器開啟時，從 input 內複製也會移除 mask literals。
  - 修正原本 editor copy 會複製畫面文字 `2026/07/10`，而不是 raw value `20260710` 的問題。
- 新增 demo 範例：
  - 在 demo 中加入可觀察 masked text-date 行為的欄位。
  - 更新 demo cache-busting version，確保瀏覽器載入新版資產。
- 補上 smoke 測試：
  - 新增 `maskEditorCopyRawWorks`，驗證 editor 內複製時 clipboard payload 是 raw digits。
  - 保留既有 mask display、input、commit 檢查。

## 驗證

- smoke test 已驗證 `maskEditorCopyRawWorks: true`。
- masked field 的顯示、輸入、提交與複製行為都有覆蓋。
- demo 已包含對應範例欄位，可用來做後續人工回歸檢查。

## 備註

- 此紀錄依據 Codex 既有 rollout memory 補寫，對應工作主題為 `fabgrid_mask_editor_copy_raw`。
- mask 相關資料流目前分成：
  - `formatMaskText()`：負責畫面顯示。
  - `getMaskDataValue()`：負責 commit / storage。
  - `getMaskCopyText()`：負責 copy semantics。
- 後續若再調整 masked editor，要同時驗證 cell copy 與 editor copy，兩者走不同事件路徑。
