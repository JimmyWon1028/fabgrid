## 完成進度

- 因 WSI FormCtrl7 外部參照 popup 的實際流程，修正 `keepFocus: false` 的 Grid editor icon 在同步交接焦點時，必填驗證將焦點搶回背景 editor 的問題；焦點交接結束後仍保留原有失焦提交與驗證行為。
- 因 WSI RepCtrl3 報表匯出需求，Excel 匯出會依數字欄位的千分位與小數位設定建立對應 number format，並維持儲存格為可運算的數值型別。
- 未指定小數位的千分位欄位匯出 Excel 時，整數不再出現尾端小數點；實際含小數的值仍保留小數顯示。
