# 2026-07-08 工作紀錄

## 完成進度

- 調整 demo 統計資訊，讓資料量顯示配合實際 rows / columns 動態更新。
- 完成 edit cell 數字欄位限制：
  - 數字欄位編輯時只允許輸入數字。
  - 支援負數。
  - 支援千分位顯示開關。
  - 支援 `precision` 固定小數位顯示，編輯完成後四捨五入。
  - 複製時不帶千分位符號。
  - validate 收到的 number 欄位值維持為數字，不是格式化字串。
- 完成日期與文字日期欄位：
  - 支援 `mask`，例如 `9999/99/99`、`9999-99-99`。
  - 日期欄位編輯時套用與文字日期相同的遮罩行為。
  - 修正 backspace 刪除遮罩文字時不能跨過分隔符的問題。
  - 日期 validate 錯誤時允許移動到下一個 cell，並在原 cell 顯示錯誤框。
- 完成 invalid item 機制：
  - 錯誤框顏色調整為 `#FFA8A8`。
  - 錯誤資料記錄於 `grid.invalidItems`。
  - `invalidItems` 改為 array。
  - 每筆 invalid item 內記錄 row / column 相關資訊。
  - 支援 column-level `validate(args)`，可同步或非同步回傳錯誤。
  - invalid item hover tooltip 調整為淺色提示，並避免遮住 cell 內容。
- 完成 combobox editor：
  - 支援下拉選取。
  - 支援直接輸入內容。
  - 支援 `limitToList`，輸入不在清單內時加入 `invalidItems`。
  - 支援 `Alt+ArrowDown` / `Option+ArrowDown` 開啟 popup。
  - 支援 `showValueInList`，popup 可顯示例如 `買賣 (1)`，其中 value 以淡灰色呈現。
  - 修正 popup 背景與寬度，避免文字過長時顯示不佳。
- 調整 demo 欄位：
  - 新增分類欄位，範例資料為 `1 - 買賣`、`2 - 加工`。
  - 增加同步 validate 與非同步 validate 範例。
  - README 補上同步 / 非同步 validate 範例。
- 完成 Excel / footer 相關調整：
  - footer 格式與 column 數字格式一致。
  - Excel export 包含 footer。
  - Excel 中金額欄位格式與畫面一致。
- 完成 demo 設定記憶：
  - grid 上方設定會寫入 localStorage。
  - 下次載入 demo 時會優先套用上次設定。
- 完成多國語系基礎：
  - 模組訊息抽出為語言檔。
  - 已提供英文、繁體中文。
  - 額外建立簡體中文語言檔，但 demo 不載入。
  - demo 支援語言切換。
  - demo grid 標題支援繁體中文 / 英文切換。
  - 語言檔分開載入，需要的語言才載入。
- 完成 header binding / header 顯示切換：
  - 模組支援熱鍵切換顯示 binding 或 header。
  - `headerToggleKey` 預設為 `false`。
  - demo 保留 `F4` 作為範例設定。
- 完成 datebox 英文月份顯示調整：
  - 英文語系月份使用英文縮寫。
- 完成排序圖示微調：
  - 排序圖示位置放在標題文字右下，靠近文字。
  - 圖示大小調整為 `8x6`。
- 完成編輯器定位修正：
  - 橫向捲動後 edit cell 位置正確維持在對應 cell 下方。
  - 保留原本編輯底色。
- 完成主題切換：
  - 參考 `src/styles/my.wijmo.*.css` 的後綴命名。
  - build 會自動產生 `dist/themes/fabgrid.<suffix>.css` 與 `.min.css`。
  - demo 新增主題切換選單。
  - demo 主題設定會寫入 localStorage。
  - 目前可切換 16 個主題：`default`、`bootstrap`、`cupertino`、`material`、`material-blue`、`material-teal`、`metro`、`metro-blue`、`metro-gray`、`metro-green`、`metro-orange`、`metro-red`、`sunny`、`pepper-grinder`、`dark-hive`、`black`。

## 驗證

- `npm run smoke` 通過。
- `node --check demo/js/grid.js` 通過。
- `node --check build/build.cjs` 通過。
- 主題清單與 `dist/themes` 產物一致，每個 demo 主題都有一般版與 min 版 CSS。
- 使用者已確認主題測試沒問題。

## 備註

- `src/styles/my.wijmo.*.css` 目前作為 FabGrid theme 產生來源。
- `dist/themes` 為 build 產物，若新增 Wijmo 參考主題，重新執行 build 即可產生對應 FabGrid theme。
- 今日工作中有不少 demo 與 dist 產物變更，提交前應再檢查 git diff，避免混入不相關內容。
