# 2026-07-10 工作紀錄

## 完成進度

- 完成群組功能與 demo：
  - `rowGroups` 支援 1 到 3 階群組。
  - demo 提供無群組、訂單編號、主要廠商加訂單編號，以及主要廠商再訂單編號的範例。
  - group row 使用 `#e1e1e1` 底色，標題採用群組欄位 header。
  - 支援展開／收合、第一個 group row 左側 cell 雙擊全部展開／收合，以及群組列選取。
  - group row checkbox 會同步選取或取消其下資料列。
- 完成欄位操作：
  - 支援多欄排序，Shift+點擊可增加排序條件；單欄排序時不顯示排序順位。
  - 支援欄位拖曳重排、header insertion indicator 與未成功插入時回彈。
  - 左上角欄位選擇器可依目前畫面順序顯示欄位，並可切換欄位可見性。
- 完成捲動與 layout 修正：
  - 修正凍結欄、row header、header、footer 與 data pane 的同步捲動。
  - 修正 Windows Chrome 未 focus 時原生捲軸 gutter 尺寸不穩定，讓 footer 的位置與寬度在 focus 前後一致。
  - 調整自訂水平與垂直捲動軸的尺寸與顏色。
- 完成編輯與 demo 範例：
  - 新增 popup grid 與 combobox 範例。
  - yymmbox 支援驗證，並新增 `grid.validateRow(rowIndex)`。
  - demo 欄位調整為主要廠商、簡稱、訂單編號、項目、日期、幣別、客戶、狀態、文字日期、年月、應付金額、分數與摘要等順序。
- 完成 Excel 匯出補強：
  - 預設匯出所有欄位；隱藏欄位仍會輸出並在 Excel 工作表隱藏。
  - 匯出目前 `view`，群組啟用時保留 group row、收合狀態與 aggregate。
  - group row 採灰底、群組標題加粗，aggregate 顯示格式與 grid 一致。
  - 群組 aggregate 會使用 grid formatter，避免 Excel 顯示未格式化的浮點小數。

## 驗證

- `npm run smoke` 通過，並已重建 `dist`。
- `git diff --check` 通過。
- `node --input-type=module -e "import('./src/fabgrid-core.js')"` 通過。
- 使用 `demo/dev.html` 比對 grid 未 focus 與 focus 後的 footer layout，footer bottom、pane 尺寸與水平位置一致。
- demo console 無 error。

## 備註

- 今日修改以 source-mode 為主，完成後已重建 `dist`。
- `demo/dev.html` 的 source query version 已隨核心行為更新，避免瀏覽器快取誤判。
