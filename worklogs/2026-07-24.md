## 完成進度

- 修正 Demo2 圖片預載展示的 Flex 交叉軸拉伸，讓 32×32 的第三張圖片維持原始尺寸。

- fabLoader 新增選用的 `vue(url)` 佇列步驟；未使用時維持零外部依賴。
- `.vue()` 呼叫時檢查 SystemJS、Vue 2 完整版與 template compiler，錯誤沿用鏈式 `catch()`。
- 建立 `demo2/` 獨立展示頁，涵蓋 CSS、script、ES module、圖片預載、HTML 快取、HTML 掛載執行 script 與 Vue 2 SFC。
- Vue 2、SystemJS 與 systemjs-vue-browser 執行檔放在 `demo2/vendor/`，Demo 不依賴 CDN。
- 執行 `npm run build:loader`，只更新 `dist/fabLoader.js` 與 `dist/fabLoader.min.js`。
- `npm test` 564/564 與 `npm run smoke:loader` 通過。
- demo2 瀏覽器測試通過；七項狀態全部完成，Vue 點擊計數互動正常，Console 無 error／warning。
- 新增獨立、零依賴的 `fabDom` browser global，第一版提供 jQuery-like `html()` 與 `append()`。
- demo2 改由 fabLoader 載入 `src/fabdom/fabDom.js`，HTML 片段以 `fabDom(...).html(...)` 動態插入連結。
- TODO 已記錄 fabDom 後續常用 DOM API 評估及未來合併到 `fabui` 的方向。
- fabDom build 規則比照 fabLoader：`build dom` 輸出 regular 與 minified，`build dom min` 只保留 minified；不納入 `build all`、逗號 scope 或 ESM。
- 執行 `npm run build:dom`，只產生 `dist/fabDom.js` 與 `dist/fabDom.min.js`。
- `npm test` 569/569 通過；瀏覽器確認 demo2 載入 `dist/fabDom.js`、動態插入連結並可觸發 alert，Console 無 error／warning。
- demo2 再由 fabLoader 載入 `dist/fabui.min.css` 與 `dist/fabui.min.js`，新增 Button 與 EditBox 的簡單互動展示。
- 瀏覽器確認 FabUI 載入狀態完成，Button 點擊後 EditBox 更新為「FabUI 已更新 1 次」，Console 無 error／warning。
- FabUI core 新增原生控制樣式隔離層，補齊 JS 動態建立的 Pivot native control class，避免一般 `button`、`input`、`label`、`select`、`textarea` 與 `<a>` selector 覆蓋元件樣式。
- 19 組 theme 以 86 個控制／狀態節點逐組驗證，通用 tag CSS 開關前後皆為 0 差異；selected／disabled 狀態與 Mono SVG icon 保持正確。
- `npm test` 576/576 與 `git diff --check` 通過；未重建 `dist/fabui.*`。
- 依使用者要求執行 `npm run build`，更新 `dist/fabui.{js,min.js,css,min.css}`、18 組外部 theme CSS 與必要圖片；14 項 build scope／native control isolation 檢查通過，未產生 ESM 或重建獨立擴充 bundle。
- demo2 加入高干擾的通用 `button`、`input`、`label`、`select`、`textarea` 與 `<a>` 樣式，並擴充 Number EditBox、CheckBox、RadioButton、SwitchButton、Tabs 與 Calendar 實際互動測試。
- 實際發現 CSS minifier 會把 `:root :is(...)`／`:root :where(...)` 錯誤壓成同一元素 selector；已修正 core 與 theme minifier，並新增 build regression test。
- 重新執行 `npm run build`；瀏覽器掃描 demo2 內 82 個 FabUI 原生控制節點，通用污染樣式命中數為 0，Button、Spinner、CheckBox、RadioButton、SwitchButton、Tabs 與 Calendar 互動皆通過。
- `npm test` 577/577 與 `git diff --check` 通過；壓縮 CSS 保留 descendant pseudo selector，未產生錯誤的 `:root:is(...)`／`:root:where(...)`。
- FabUI core 隔離擴充到 `div`、`span`、清單、表格、SVG、圖片及其他結構節點；所有 core component、icon 與 19 組 theme selector 統一加入 `:root` scope，獨立擴充元件未納入。
- 共用隔離層只處理 FabUI renderer root 與其內部 class 節點，刻意不重設 Tooltip target、Form 一般欄位或 Panel／Tabs／Accordion 的使用者 HTML 內容。
- demo2 新增 Accordion、MenuButton 與 Tooltip，並以高干擾結構標籤樣式測試；119 個可見節點污染命中為 0，新增元件互動正常。
- Source browser fixture 擴充到 118 個控制／結構節點；19 組 theme 開關通用標籤 CSS 前後均為 0 差異。
- 執行 `npm run build` 更新 FabUI core 與 theme dist；`npm test` 577/577 通過。
- 修正共用隔離層的廣域 pseudo-state reset 誤蓋 EditBox hover 邊框與底色；保留既有 native tag 基礎隔離，並加入實際 EditBox 結構回歸 fixture。
- Source browser 在正常與高干擾通用 CSS 下驗證 EditBox hover／focus 樣式正常；`npm test` 577/577 與 `git diff --check` 通過，未重建 `dist`。
- 依使用者要求執行 `npm run build` 更新 FabUI core 與 theme dist；demo2 正式 dist 路徑確認 EditBox hover 邊框／底色正常，Loader 八項狀態全部完成。
- demo2 互動驗證通過：EditBox 更新、Number Spinner、RadioButton、Tabs 與 Vue 計數均正常，Console 無訊息；`npm test` 577/577、無 ESM 輸出且 `git diff --check` 通過。
- 回查共用隔離層對其他元件與 theme 狀態的影響；確認過度 reset 是全域單一來源，移除後 Button、Spinner、CheckBox、RadioButton、SwitchButton、Tabs、Calendar、Accordion、MenuButton 與 popup 狀態均恢復由元件 theme 控制。
- 正式 dist Demo 抽查 Black、Material Blue、Metro Red、Mono Green 四個 theme 家族，EditBox、trigger、spinner、icon 與 hover 邊框正常，未發現第二個廣域狀態覆蓋規則。
- fabDom 升級為 0.2.0，新增 jQuery-like `text()`、`val()`、`attr()` 與 `css()` getter／setter、callback setter，以及 `attr({...})`／`css({...})`；不擴充其他 jQuery API。
- `css()` 數值依 unitless property 決定是否補 `px`，`attr(name, null)` 與 `css(name, null)` 分別移除 attribute／inline style；README、TODO 與專案契約已同步。
- 執行 `npm run build:dom`，只更新 `dist/fabDom.js` 與 `dist/fabDom.min.js`；demo2 正式 dist 驗證版本 0.2.0 與 `text()`、`val()`、`attr()`、`css()` 均正常，Console 無訊息。
- `npm test` 582/582、`git diff --check` 與無 fabDom ESM 輸出檢查通過。
- 修正 SwitchButton 內部 ON／OFF／handle 文字被共用隔離層重設行高後無法垂直置中的問題；source Demo 實測 30px／36px 尺寸皆置中，14 項相關測試通過，未重建 `dist`。
- 依使用者要求執行 `npm run build`，更新 FabUI core、18 組外部 theme regular／minified CSS 與必要圖片；core smoke、21 項 SwitchButton／theme／native isolation 測試、JS 語法、無 ESM 與 `git diff --check` 通過。完整測試 575/577，兩項既有失敗為 fabDom 原始碼 `0.2.0` 與測試預期 `0.1.0` 不一致。
- fabDom 暫時保留；`demo2/` 改由 fabLoader 載入本機 jQuery 4.0.0 slim minified，HTML 片段與 Demo DOM 操作改用 `$`，FabUI 與 fabLoader 仍維持零 jQuery 依賴。
- demo2 瀏覽器實測八項 Loader 狀態全部完成，jQuery 版本為 `4.0.0+slim`，fabDom 未載入；HTML script、FabUI Button 與 Vue 計數互動正常，Console 無 error／warning。
- `npm test` 581/581 與 `git diff --check` 通過；本次未修改 fabLoader 或 fabDom 原始碼，未重建其 dist。
- fabDom 升級為 0.3.0，完成第一階與第二階 jQuery-like DOM API：集合操作、內容與插入、attribute／property、class、直接與委派事件、移除及常用 traversal。
- fabDom 載入時只有在頁面沒有 jQuery 且 `$` 尚未被占用時才掛載 `$` 別名；既有 jQuery 或其他 `$` 均不覆蓋，`window.fabDom` 永遠保留。
- 新增獨立 dist browser smoke，確認 `$` 別名、class、attribute、CSS、直接／委派事件、插入與 traversal 正常，Console 無 error／warning。
- 執行 `npm run build:dom`，只更新 `dist/fabDom.js`（23,060 bytes）與 `dist/fabDom.min.js`（10,040 bytes），未產生 ESM 或重建其他 dist。
- `npm test` 589/589、JS 語法與 `git diff --check` 通過；依使用者指示，本次未修改 `demo2/`，等待後續 Demo 調整方式。
- `demo2/` 改由 fabLoader 先載入本機 jQuery 4 slim、再載入 `dist/fabDom.js`；jQuery 載入行附上可直接註解的提示，讓同一頁可切換 `$` 的提供者。
- 瀏覽器分別驗證兩種模式：預設模式 `$` 使用 jQuery 4.0.0 slim；暫時註解 jQuery 載入行後 `$` 使用 fabDom 0.3.0。兩種模式的 Loader 八項狀態、HTML 掛載 script 與 FabUI／Vue 展示均完成，且頁面無錯誤狀態。
- fabLoader 升級為 0.4.0，新增 `loadText()`、`getText()` 與 `loadXml()`；文字、HTML 與 XML 原文共用同一份 URL／credentials 快取，既有 `loadHtml()`／`getHtml()` 保持相容。
- `loadXml()` 每次由快取原文解析新的 `XMLDocument`，避免呼叫端共享可變 Document；XML 格式錯誤時 Promise reject，已補齊有效／無效 XML 與 TXT／HTML 共用快取測試。
- 執行 `npm run build:loader`，只更新 `dist/fabLoader.js`（15,240 bytes）與 `dist/fabLoader.min.js`（7,010 bytes）；`npm run smoke:loader` 實際驗證 TXT、XML 與既有資源載入通過。
- `npm test` 590/590、JS 語法、無 ESM 與 `git diff --check` 通過；Diagram source／dist Demo 及 CDN 測試頁同步更新 Loader query version。
- 修正 fabDom 0.3.0 的插入方法將 `HTMLImageElement` 字串化成 `[object HTMLImageElement]`，造成 Demo2 在停用 jQuery 時圖片狀態完成但畫面沒有實際圖片的問題。
- fabDom 升級為 0.3.1；`append()`／`prepend()`／`before()`／`after()` 現在同時接受 HTML 字串、DOM Node、fabDom collection 與 NodeList，多目標插入沿用 jQuery 的複製／原始節點規則。
- 執行 `npm run build:dom`，只更新 `dist/fabDom.js`（25,114 bytes）與 `dist/fabDom.min.js`（10,766 bytes）；Demo2 保持 jQuery 載入行為註解，並更新 fabDom query version。
- Demo2 瀏覽器確認 `$` 使用 fabDom 0.3.1、圖片區包含實際 `<img>` 且八項狀態全部完成；`npm test` 591/591 與 `git diff --check` 通過。
- fabLoader 升級為 0.5.0，新增 `script`／`css`／`image`／`text` 四個集中設定桶；使用者可在載入前呼叫 `setConfig()`，未設定時沿用內建預設，`getConfig()` 回傳不暴露內部狀態的獨立副本。
- 四桶預設 timeout 為 30 秒，文字 credentials 保持 `'same-origin'`；重要 options 納入快取鍵，失敗、逾時與取消都會清除該筆紀錄，新增 `cancel(bucket?, url?)`。
- 圖片仍共用同一次預載請求，但每次 `preloadImage()` 回傳不同 `<img>`；文字快取清理 API 統一為 `clearTextCache()`，移除 `clearHtmlCache()`，XML 解析失敗也會清除原文快取。
- 執行 `npm run build:loader`，只更新 `dist/fabLoader.js`（28,030 bytes）與 `dist/fabLoader.min.js`（12,366 bytes），未產生 ESM 或重建其他 dist。
- `npm run smoke:loader` 實際瀏覽器驗證通過；`npm test` 596/596、JS 語法與 `git diff --check` 通過。
- Demo2 保持 jQuery 載入行註解，實際瀏覽器確認 `$` 由 fabDom 0.3.1 提供、八項 Loader 狀態全部完成、圖片節點存在且 Vue 計數互動正常。
- Demo2 圖片預覽改為對同一 URL 呼叫兩次 `preloadImage()`，確認回傳不同 DOM 節點後將同一張圖片並排插入兩次。
- Demo2 HTML 掛載改為 `loadHtml()` 取得文字後以 `$('#html-demo').html(html)` 插入；片段維持純 HTML，後續連結與狀態更新集中在同一個 `$` callback。
- Demo2 錯誤狀態統一以 `$('body').addClass('has-error')` 設定，移除 jQuery／fabDom 的分支與原生 `classList` 寫法。
- fabLoader 升級為 0.6.0；`preloadImage([url, ...])` 平行預載並回傳真正的 Array，每次讀取同一數字索引都自動建立來源相同、DOM identity 不同的 `<img>`。
- Demo2 改為保存 `window.myImages`，直接將 `myImages[0]` append 到兩個不同容器，不再要求使用者呼叫 `cloneNode()` 或額外的 create API。
- 執行 `npm run build:loader`，只更新 `dist/fabLoader.js`（28,737 bytes）與 `dist/fabLoader.min.js`（12,729 bytes）；瀏覽器 smoke、`npm test` 597/597、JS 語法、無 ESM 與 `git diff --check` 通過。
- Demo2 將圖片陣列映射為命名的 `myImage.loader` 與 `myImage.unlock` getter；名稱更清楚，同時保留每次讀取都由 fabLoader Array getter 提供不同 `<img>` 的行為。
- fabLoader 升級為 0.7.0；`preloadImage({ name: url })` 可直接回傳保留名稱的可重複圖片物件，與既有單一 URL、陣列輸入並存。
- Demo2 改由 Loader 直接建立 `myImage.loader`／`myImage.unlock`，頁面不再自行撰寫 getter。
- 執行 `npm run build:loader`，只更新 `dist/fabLoader.js`（29,395 bytes）與 `dist/fabLoader.min.js`（13,018 bytes）；瀏覽器 smoke、`npm test` 598/598、JS 語法、無 ESM 與 `git diff --check` 通過。
- Demo2 載入失敗不再彈出 `alert()`，改由 `console.error()` 以瀏覽器 Console 錯誤樣式輸出訊息與原始 Error。
- Demo2 的「FabUI 元件與通用 CSS 隔離」縮減為圖示要求的四張卡片，只保留 EditBox／Button、Number EditBox／Spinner、CheckBox／RadioButton 與 SwitchButton；移除其他 FabUI 展示、初始化程式與專用 CSS。
- fabLoader 升級為 0.8.0，新增選用的 `react(url)` 佇列步驟；呼叫時檢查 SystemJS、React 與 ReactDOM Client，未使用時維持零 React 依賴。
- Demo2 保留原本只管理 Vue 的 `systemjs.config.js`，另以 `runtime.config.js` 的 `// for react` 區塊設定本機 JSX loader；新增 React 19 `hello.jsx` 計數元件。
- React 19、ReactDOM Client 與 systemjs-plugin-babel 執行檔及授權文件皆放在 `demo2/vendor/`，Demo 不依賴 CDN。
- 執行 `npm run build:loader`，只更新 `dist/fabLoader.js` 與 `dist/fabLoader.min.js`；23/23 Loader 測試與瀏覽器 smoke 通過。
- Demo2 瀏覽器確認 Vue 2 SFC 與 React JSX 狀態完成，React 按鈕由 0 更新為 1，所有 Loader 狀態完成且 Console 無錯誤。
- fabDom 0.4.0 實作納入 fabLoader 0.9.0 發佈檔，公開 `fabLoader.dom()` 並保留不覆蓋既有 jQuery／`$` 的安全別名；獨立 fabDom build 暫時保留相容。
- 新增 jQuery-like `load(url, options?, callback?)`，回傳原 collection，callback 使用 `(responseText, status, result)`，實際載入、快取與片段 script 執行完全橋接 `fabLoader.mountHtml()`；Demo2 不再另行載入 `dist/fabDom.js`。
- 執行 `npm run build:loader` 與 `npm run build:dom`，只更新兩組 regular／minified browser global；Loader smoke、Demo2 實際瀏覽器驗證、`npm test` 602/602、JS 語法、無 ESM 與 `git diff --check` 全部通過。
- 合併方向確認後移除已無用途的 `dist/fabDom.js`、`dist/fabDom.min.js`、獨立 `build:dom` 與 fabDom browser smoke；`src/fabdom/fabDom.js` 保留為 fabLoader build 的共用原始碼。完整測試 601/601 與 `git diff --check` 通過。
- 檢查 Demo2 第一方範例的 DOM 操作並統一優先使用 `$`：React root 改用 `$('#react-demo').get(0)`，HTML 掛載優先使用 `$htmlDemo.load()`，只有啟用不含 Ajax `.load()` 的 jQuery slim 時才退回 `fabLoader.dom()`；新增靜態測試禁止 Demo2 範例直接使用 `getElementById()`／`querySelector()`。實際瀏覽器確認九項狀態全部完成，Console 無 warning／error。
- fabLoader 升級為 0.10.0，新增 `useDom()`：有效 jQuery 存在時回傳 jQuery，否則回傳內建 fabDom，且不修改目前全域 `$`。Demo2 以一次 `fabLoader.useDom()` 取得局部 `$`，移除頁面自行判斷 jQuery／fabDom 與 provider 標記的程式；Loader smoke 與 Demo2 實際瀏覽器九項狀態通過，Console 無 warning／error。
- fabLoader 升級為 0.11.0，新增 `wait(callback)` 作為 `run(callback)` 的相容別名；頂層與鏈式 API 都共用同一函數，保留相同的佇列暫停與錯誤傳遞行為。
- fabLoader 升級為 0.12.0，`loadHtml()` 比照 `preloadImage()` 支援單一 URL、URL 陣列及名稱 URL 物件；集合平行載入、保留陣列順序／物件名稱，並繼續共用既有文字快取。
- 新增 `docs/fabloader-api.md` 完整 API 操作手冊，記錄 0.12.0 的載入佇列、`run()`／`wait()` 等待規則、四桶設定、快取／取消、圖片與 HTML 集合、HTML 掛載、Vue／React optional runtime、內建 DOM helper、錯誤處理及 Loader 專用 build。
